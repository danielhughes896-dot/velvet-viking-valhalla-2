{
  "name": "velvet-viking-valhalla",
  "version": "1.0.0",
  "private": true,
  "description": "Native app tooling (Capacitor) for the Velvet Viking training block generator. The web app itself is a static file (velvet-viking-valhalla.html) deployed to Vercel and is not built by this package.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "cap:sync": "cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/share": "^8.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
